run = "npm install && npm run build && npm run start"

[deployment]
run = ["npm", "install", "&&", "npm", "run", "build", "&&", "npm", "run", "start"]
deploymentTarget = "preview"
ignorePorts = false

[[ports]]
localPort = 3000
externalPort = 80
